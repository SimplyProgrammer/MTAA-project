import React from "react";
import { View, Text } from "react-native";
import { Screen } from "@/components/styles";

export default function TeacherOverviewScreen() {
	return (
		<View className={`${Screen}`}>
			<Text>Teacher overview</Text>
		</View>
	);
};